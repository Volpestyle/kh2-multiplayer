# Memory / RE probes

Live probe notes, CE MCP logs, and session write-ups for reverse-engineering KH2 FM PC.

| Item | Description |
|------|-------------|
| `RE_PROBE_CHECKLIST.md` | Bridge must-haves before networking work |
| `memory_probe_log_*.txt` | Timestamped MCP / manual captures |
| `archive/` | Older position-probe session handoffs (`POSITION_PROBE_SESSION*.md`) |

Main offset map stays at `../pointer_map_v1.md`; runtime constants in `../../runtime/include/kh2coop/KH2Offsets.hpp`.
